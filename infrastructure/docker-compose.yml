services:
  mysql:
    image: mysql:8.4
    container_name: feeleats-mysql
    restart: always
    ports:
      - "13306:3306"
    volumes:
      - ./db/mysql/data:/var/lib/mysql
    environment:
      MYSQL_ROOT_PASSWORD: root
